- module: system
  metricsets:
    - cpu
    - memory
  #  - network
   # - diskio
    #- filesystem
    #- load
    - uptime
  period: 60s
  processes: ['.*']
  cpu.metrics: ["percentages"]
  enabled: true
